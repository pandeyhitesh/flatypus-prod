enum Level { small, medium, large }

enum TaskSchedule { doNotRepeat, daily, customWeek, alternateDays, monthly }

enum ExpenseTransactionType { toPay, toGet }

enum AuthStatus { loggedIn, loggedOut }

enum UserRole { ADMIN, MEMBER }
