import 'package:flutter/material.dart';

class CreateExpenseItemScreen extends StatelessWidget {
  const CreateExpenseItemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
